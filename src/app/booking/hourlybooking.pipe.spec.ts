import { HourlybookingPipe } from './hourlybooking.pipe';

describe('HourlybookingPipe', () => {
  it('create an instance', () => {
    const pipe = new HourlybookingPipe();
    expect(pipe).toBeTruthy();
  });
});
